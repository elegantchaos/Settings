# Settings

`Settings` is a Swift package for typed application settings and storage helpers.

## Contents

- Strongly typed setting keys
- `UserDefaults` and `AppStorage` integration
- Supporting test coverage for settings behavior

